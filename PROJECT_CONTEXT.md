# 项目说明

这是一个宠物调度系统（pet-dispatch-pro），主要参考黑马“苍穹外卖”和“黑马点评”的业务与技术思路，面向宠物上门服务、宠托师接单、订单调度和履约存证场景。

## 当前进度

- 已完成用户模块雏形：手机号验证码登录、Redis 验证码缓存、JWT 登录态。
- 已完成宠物档案模块雏形：新增、查询、删除当前用户宠物。
- 已完成宠托师模块雏形：用户申请成为宠托师、管理员审核宠托师。
- 正在开发订单模块：创建订单、模拟支付、公共订单池、宠托师抢单、开始服务、完成服务。
- 第一轮急救修复已完成：宠物删除水平越权、抢单时 User.id 和 Sitter.id 混用。
- 第二阶段基建推进中：已引入 DTO、Spring Validation、MapStruct，先覆盖宠物新增、宠托师申请、订单创建三个写接口。
- 配置与交付基建推进中：已外置数据库、Redis、JWT 配置，新增本地配置示例和 `docs/schema.sql`。
- 已补充数据库交付说明 `docs/db-setup.md`，区分全新建库、旧库按 migration 升级和演示数据执行顺序；迁移脚本已补齐目标库选择。
- 已清理 `application.yml` 乱码注释并补充 README 本地启动前检查项，保证配置说明和实际 `local` profile 启动方式一致。
- 第三阶段 Service 层重构已启动：宠物模块已从 Controller 直连 Mapper 调整为 Controller -> Service -> Mapper。
- 宠托师申请与管理端审核已迁移到 `SitterServiceImpl`，Controller 只负责参数接收和返回包装。
- 订单创建、支付、公共池、抢单、开始服务、完成服务已迁移到 `OrdersServiceImpl`，保留原有状态流转条件。
- 订单表已规划 `version` 乐观锁字段，并在抢单/履约状态流转时推进版本号。
- 用户验证码登录、自动注册和 JWT 生成已迁移到 `UserServiceImpl`。
- 已补充宠物档案与宠托师服务层单测，覆盖宠物归属隔离删除、用户宠物列表查询、宠托师重复申请拦截和审核状态流转。
- 已补充订单履约权限隔离单测，覆盖开始/完成服务时必须由当前登录用户解析真实 `sitter.id`，并按 `order.id + sitter_id + status` 推进状态与乐观锁版本。
- 已修复订单创建宠物档案越权风险：创建订单前校验 `pet_info.id + user_id` 归属，不允许使用他人宠物档案下单，并通过统一异常处理返回业务错误。
- 已落地轻量角色权限基础：`user.role` 支持 `USER/SITTER/ADMIN`，登录 JWT 写入角色，拦截器写入 `BaseContext`，`/api/admin/**` 已增加管理员角色拦截，宠托师审核通过后同步升级用户角色为 `SITTER`。
- 已收紧宠托师履约权限：开始服务、完成服务同抢单一样要求当前用户存在审核通过的宠托师档案，并补充未审核宠托师不可履约的单测。
- 已启动 VO 出参隔离：宠物列表使用 `PetInfoVO`，订单创建与公共订单池使用 `OrderVO`，Controller 不再直接向外返回宠物/订单 Entity；订单出参已补充 `statusDesc`，宠托师出参已补充 `workStatusDesc/auditStatusDesc`，便于前端和接口演示直接展示状态文案。
- 已补充订单查询接口：用户可查询自己的订单列表，审核通过的宠托师可查询自己承接的服务订单列表，接口统一返回 `OrderVO`。
- 已补充订单取消接口：仅允许当前下单用户取消待支付或待接单订单，取消时通过条件更新推进订单状态为已取消并递增乐观锁版本。
- 已补充轻量宠托师工作台：宠托师可查询自己的工作档案 `SitterVO`，审核通过后可在休息中/接单中之间切换工作状态，支撑“上线接单 -> 抢单履约”主链路。
- 已补充轻量订单评价闭环：用户可在订单服务完成后提交 1-5 分评分和评价内容，订单状态从服务完成推进到已评价并递增乐观锁版本。
- 已收口履约凭证入参：开始服务/完成服务改为接收 `OrderProofDTO`，通用上传接口显式接收 `file` 并增加空文件/空文件名校验，OSS 文件名生成兼容无后缀文件。
- 已新增 `README.md`，围绕核心业务流程、技术栈、本地配置、常用接口和测试命令整理轻量项目说明。
- 已补充账号状态安全校验：`user.status=0` 的封禁用户即使验证码正确也不能获取 JWT 登录态。
- 已新增 `docs/demo-data.sql`，提供用户、宠物、宠托师和订单核心状态的本地演示数据。
- 已补充接口级冒烟测试：使用 MockMvc 覆盖订单创建/履约/评价接口 JSON 入参和参数校验、订单列表/取消/抢单、宠托师申请/工作台、管理端审核、用户登录/验证码发送、宠物档案新增/查询/删除和通用上传成功/失败分支接口，并补充订单/宠托师 VO 状态文案映射测试，测试数提升到 107。
- 已新增 `docs/demo-flow.md`，按登录、宠物、宠托师入驻、订单调度、查询取消整理核心演示步骤。
- 已润色演示文档：`docs/demo-flow.md` 已补充宠物新增、宠托师申请、订单创建、履约打卡和评价的关键 JSON 请求体，`README.md` 已说明订单/宠托师出参包含中文状态文案。
- 已校准演示数据文档一致性：`docs/demo-flow.md` 已补充 `docs/demo-data.sql` 中的固定演示 ID，并将宠物/订单请求示例对齐到 `2001/4001` 等实际演示数据。
- 已新增 `docs/release-checklist.md`，汇总代码检查、本地环境、数据库脚本和核心业务演示检查项，便于最终交付前逐项确认。
- 当前核心 MVP 暂不需要新增业务表，`user/pet_info/sitter/orders` 已能支撑主流程；后续仅在独立评价、文件凭证台账等扩展需求出现时再拆新表。
- 已补充管理端待审核宠托师列表接口 `GET /api/admin/sitter/pending`，复用 `sitter` 表按待审核状态查询，便于演示管理员审核流程。
- 已补充订单详情查询接口：用户可查询自己的订单详情，宠托师可查询自己承接的订单详情，均按身份做数据隔离并返回 `OrderVO`。
- 已补充宠托师工作状态随订单流转：抢单成功后切为服务中，完成服务后切回接单中并累加接单数，订单与宠托师状态更新放入事务边界。
- 已集中宠托师审核状态和工作状态码到 `SitterAuditStatusEnum/SitterWorkStatusEnum`，Service 层状态判断与 VO 状态文案复用同一套状态定义。
- 已收紧模拟支付权限：支付订单时按 `orderSn + 当前用户 userId` 查询待支付订单，避免通过订单号支付他人订单。
- 已补充一致性收口：订单抢单/完成服务后若宠托师状态或统计更新失败会抛出业务异常触发事务回滚；宠托师审核只允许处理待审核记录，避免重复审核。
- 已补充订单服务层一致性测试：覆盖支付查不到当前用户订单、并发/重复抢单导致订单条件更新失败、抢单后宠托师状态更新失败、完成服务后宠托师统计更新失败等核心分支。
- 已收紧宠托师审核通过的一致性：审核通过后升级用户角色失败会抛出业务异常并触发事务回滚，避免 `sitter` 已通过但 `user.role` 未升级的半完成状态。
- 已收紧宠托师审核并发边界：审核状态更新按 `id + audit_status=待审核` 条件执行，避免并发重复审核同一条申请。
- 已收紧抢单自接单风险：公共订单池过滤当前用户自己的订单，抢单条件更新也排除当前用户自己的订单，避免宠托师身份用户承接自己下的订单。
- 已收紧公共订单池权限：仅审核通过的宠托师可查看待接单公共订单池，普通用户和未审核宠托师返回空列表；真正抢单时仍要求宠托师不是休息中。
- 已收紧登录拦截器上下文清理：无效 token 分支会主动清理 `BaseContext`，请求结束后同样清理用户 ID 和角色，避免线程复用时残留旧身份。
- 已集中 Controller 用户可见提示文案到 `ApiMessages`，减少接口返回消息硬编码，保持现有接口响应内容不变。
- 已移除用户模块调试写接口 `GET /api/user/test` 及其专用 `UserVO/UserConverter`，交付版用户模块仅保留验证码发送和登录主流程。
- 已补充全局异常兜底：参数校验和业务异常保留明确提示，未知异常统一返回“系统繁忙，请稍后再试”，避免泄露内部错误细节。
- 已重写 `docs/demo-flow.md` 和 `docs/release-checklist.md` 为清晰交付版，并新增 `docs/final-summary.md` 作为最终项目讲解摘要。
- 已为核心接口和复杂业务逻辑补充中文注释：覆盖用户登录、宠物档案、宠托师申请/审核、订单调度、登录拦截、管理员权限和全局异常处理，便于后续阅读和讲解。
- 已重新梳理宠托师入驻演示链路：申请入驻只允许正常状态的普通 `USER` 提交，且申请手机号必须等于当前登录手机号；演示数据新增待审核申请用户 `13800138004/1004` 与待审核档案 `3002`，管理员待审核列表可稳定展示数据。
- 已补充轻量宠托师推荐能力：订单评价成功后更新宠托师平均评分，并写入 Redis ZSET `pet:sitter:rank:rating`；新增高分宠托师推荐接口 `GET /api/sitter/recommend/top-rated`，榜单为空时回源数据库按评分和接单量排序。
- 已收口宠物档案修改能力：新增 `PUT /api/pet/{id}`，按 `pet_info.id + 当前 userId` 限制只能修改自己的宠物，前端调试台同步补充“修改宠物”入口。
- 已优化验证码演示输出：用户验证码发送仍走 Redis 缓存，但从 `System.out` 改为标准日志输出，方便后续接入真实短信服务或统一日志采集。
- 已进入完整前端阶段：`pet-dispatch-pro-frontend` 已从轻量调试台升级为单页业务工作台，按用户端、宠托师端、管理员端、推荐记录拆分模块，并通过本地 Node 代理与后端 `/api/**` 完整交互。
- 已补齐宠物档案字段：`is_neutered` 和 `aggressive_tag` 已接入 Entity/DTO/VO/新增/修改/演示数据/前端下拉框；抢单资格调整为审核通过且非休息中，避免宠托师本地状态停留在服务中时阻断演示抢单。
- 已补充指定宠托师业务闭环：宠物档案支持图片 URL；订单支持 `target_sitter_id` 指定宠托师，公共池只展示未指定订单，宠托师可查看指定给自己的订单并选择接单或拒单；推荐模块支持查看宠托师历史评价。
- 已优化完整前端交互：高分宠托师卡片可直接查看详细评价或填入指定宠托师 ID；指定订单卡片可直接同意接单或按预设原因拒单，拒单提示明确模拟取消、退款和通知用户。
- 已优化凭证和评价展示：前端将开始服务凭证、结束服务凭证拆分为两个 URL，并支持分别上传回填；宠托师评价详情突出展示 `evaluate_content` 文本评价，模拟电商评价区的查看体验。
- 已优化前端数据展示和跳转：推荐与记录模块补充我的宠物、我的历史订单入口，切换模块时自动刷新当前用户统计；订单状态、宠物类型、绝育状态、攻击性标签转为中文展示；历史订单支持再次选择已服务宠托师，评分改为 1-5 分下拉框。
- 已优化公共订单池和宠物图片上传：审核通过的宠托师即使处于休息中也可以查看公共订单池和指定给自己的订单，真正抢单或同意接单时再校验工作状态；前端公共池订单卡片可直接点击“抢这个订单”；宠物图片支持从磁盘选择上传并自动回填图片 URL。
- 已重构前端为真实业务页面：访问后先进入验证码登录页，登录后进入首页、宠托师选择、个人中心、工作台、管理审核；个人中心展示历史订单和宠托师注册状态，工作台按是否已注册宠托师展示入驻提示或公共订单池/指定订单/服务订单。
- 已优化真实前端调试体验：宠物档案支持修改并可在修改弹窗内上传新图片，个人中心宠物展示改为精简列表；开始服务和完成服务改为点击订单按钮后弹窗上传凭证再提交；抢单规则明确为宠托师同一时间只能承接一个订单，后端仅允许接单中宠托师抢单，服务中或休息中会被拒绝。

## 技术栈

- Spring Boot 3.2.5
- Java 21
- MyBatis-Plus
- MySQL
- Redis
- Knife4j
- Hutool
- Lombok
- 阿里云 OSS
- MapStruct
- Spring Validation

## 当前数据库

- `user`：C 端用户表。
- `pet_info`：宠物数字档案表。
- `sitter`：W 端宠托师工作档案表，当前通过 `user_id` 绑定普通用户身份。
- `orders`：核心调度订单表，当前 `sitter_id` 绑定真实的 `sitter.id`，不能绑定 `user.id`。
- 当前表结构已整理到 `docs/schema.sql`。

## 下一步目标

- 继续围绕核心调度链路收口，下一步可优先做接口返回消息统一、少量边界测试或演示文档润色，避免扩展过多后台 CRUD。
- Service 层主干重构已覆盖用户、宠物、宠托师、订单模块，下一步优先补充更多核心链路测试和权限隔离。
- 第四阶段加强权限与安全：引入 Sa-Token、区分用户/宠托师/管理员权限、外置密钥和本地配置。
- 当前已先用项目内轻量角色体系保护管理端接口，后续可继续迁移到 Sa-Token 或补充更细的接口权限注解。
- 第五阶段完善交付标准：后续可在 `docs/schema.sql` 基础上接入 Flyway，补充越权删除和并发抢单测试。
